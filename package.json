{
  "name": "StrongInduction",
  "version": "1.0",
  "description": "Provides a strong induction lemma",
  "esy": {
    "build": "dune build -p #{self.name}",
		"install": "dune install --prefix=#{self.install}",
    "buildsInSource": "_build"
  },
  "dependencies": {
    "@opam/dune": "*",
    "@opam/coq": "*"
  }
}

